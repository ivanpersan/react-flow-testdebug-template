//@flow
export class TestService {
    static sumTwoNumbers(firstNumber: number, secondNumber: number): number {
        var result: number = firstNumber + secondNumber;
        return result;
    }
}